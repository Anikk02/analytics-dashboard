name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Generate raw data
        run: python backend/scripts/generate_data.py

      - name: Clean data
        run: python backend/scripts/clean_data.py

      - name: Analyze data
        run: python backend/scripts/analyze.py
      - name: Run pytest
        run: pytest -v